This application is for taking C code from a client through a webpage, transferring it to a server PC, compiling the program using GCC, executing it and returning output if compilation successful, else returning the error message.
